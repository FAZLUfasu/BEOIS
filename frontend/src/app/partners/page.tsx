import {
  PartnersWorkspace,
} from "@/components/partners/partners-workspace";

import {
  AppShell,
} from "@/components/layout/app-shell";

export default function PartnersPage() {
  return (
    <AppShell>
      <PartnersWorkspace />
    </AppShell>
  );
}